import React from 'react';
import {useParams} from 'react-router-dom';

const QuestionPage = () => {
    const {id} = useParams();
    return (
        <div>

        </div>
    )
}

export default QuestionPage;
